# Specs

Put each spec in a folder: `YYYY-MM-DD-HHMM-feature-slug/` with `plan.md`, `shape.md`, `standards.md`, `references.md`, and optionally `visuals/`.

Use the **/shape-spec** command (in .cursor/commands/spec-bot/) to create new specs.
