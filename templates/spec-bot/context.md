# Project State Summary

Update this file whenever you change `spec-bot/state.json` so new chats stay in sync. Full state: `spec-bot/state.json`.

---

## Phase

(current phase: planning / features / tasks / implementing / qa / done)

## Product

(one-line product description)

## Features

(count and list — e.g. "3 features: f1: Auth, f2: Dashboard, f3: Settings")

## Tasks

(count and status — e.g. "8 tasks: 3 done, 2 in-progress, 3 pending")

## Last Action

(what was last done — e.g. "Implemented t3: Login API endpoint")

## Next Step

(recommended next command — e.g. "Run /implement-task to continue with t4")
